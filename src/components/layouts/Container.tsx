import { StyleSheet, Text, View } from "react-native";

const Container = () => {
  return (
    <View>
      <Text>Container</Text>
    </View>
  );
};

export default Container;

const styles = StyleSheet.create({});
